# 坡地韌性哨兵 — 架構說明

部署：https://rhynowu-ardswc-resilience-observatory.hf.space
（Hugging Face Space，`docker` SDK，`cpu-basic` 硬體，單一 Flask process）

本文件說明**這個公開倉庫**實際部署的系統架構。完整開發過程、每項功能的實測依據與
除錯記錄維護在私有研究倉庫；這裡只記錄「現在系統長什麼樣、為什麼這樣設計」。

## 1. 這是什麼

一個台灣坡地/河川/海岸長期不穩定地點的衛星歷史影像變遷觀測站，核心方法論：

```
水保署 ARDSWC 全台災害影像通報（97,500+ 張現場照片）
  → 自建網格掃描 GetEventPositionList API（突破單次查詢 500 筆上限）
  → 76,773 筆全台去重事件紀錄
  → 依「不同年份出現次數」（非原始照片數）排序 → Top 100 長期複發熱點
  → 對每個熱點跑 Google Earth Web 歷史影像擷取（最長回溯 25 期）
  → SSIM 像素級變遷偵測（ge_change_detect.py，唯一比對引擎，不重寫演算法）
  → 11 個熱點做完整人工目視覆核 → 深度驗證台帳
```

這不是一次性技術展示，而是一個把「機器只建議、人工覆核才是權威」的治理原則落實到每個
功能裡的系統——包括誠實記錄自動化比對在哪些案例上會失準（見「深度驗證台帳」分頁）。

## 2. 系統架構

單一 Flask 後端（`webapp/change_detect_viewer/app.py`）+ 單頁 Jinja/Leaflet 前端
（`templates/index.html`，無前端框架/建置流程，純 HTML+CSS+原生 JS）。無資料庫——所有
權威資料是靜態 JSON 檔（`data/ardswc_hotspots/`）與擷取/計算後落地的影像檔
（`data/ge_captures/`），API 是這些檔案的薄包裝層。

```
瀏覽器 ── HTTP ── Flask (app.py)
                     ├─ 靜態 JSON 讀取（熱點清單/台帳/事件統計/分類地點）
                     ├─ ge_change_detect.py（SSIM 比對，讀取已快取的影像/JSON）
                     ├─ ge_web_capture_v2.py（線上分析：即時 GE Web 擷取，見 §4）
                     ├─ cesium_terrain.py（地形資料：等高線疊圖／坡度流域分析）
                     ├─ watershed_analysis.py（D8 流向＋流量累積，積水候選提示）
                     ├─ ardswc_photo_search.py（水保署歷史影像庫真實查詢）
                     └─ gmaps_tiles.py（Apple／國土測繪中心底圖圖磚代理）
```

## 3. 五個分頁

| 分頁 | 內容 |
|---|---|
| **觀測導覽** | Landing page——7 大類真實地點的卡片瀏覽（§5），點卡片直接跳進「線上分析」面板 |
| **觀測站首頁** | 統計總覽＋76,773 筆事件分類/年份統計＋可切換底圖的熱點地圖（含「線上分析」面板） |
| **熱點總覽** | 100 個熱點的可排序清單，含巡查優先級 A–D（§6） |
| **深度驗證台帳** | 11 個熱點的完整人工目視覆核紀錄——刻意記錄「哪裡失準」而非只秀成功案例 |
| **發現與建言** | 政府/民眾/公共政策三方向的靜態建言內容 |

## 4. 線上分析：任意座標即時比對

不限於既有 100 個熱點——使用者可在地圖上點選（或從 §5 的分類清單挑選）任意座標，即時
對該座標跑 GE Web 歷史影像擷取＋全部相鄰日期的 SSIM 變遷偵測，用的是與 100 熱點同一套
比對引擎。

- **擷取後端**：`scripts/ge_web_capture_v2.py`，Playwright 驅動 Google Earth Web，操作
  Flutter/CanvasKit 渲染的無障礙功能樹（`flt-semantics`/`[role]` 節點）讀取歷史日期
  stepper、進歷史模式、逐步取得每個日期的截圖。
- **容器模式**（`GE_CAPTURE_CONTAINER_MODE=1`，這個公開部署的預設值）：純 headless
  Chromium（Playwright 內建，非需要系統安裝的 Google Chrome）、一次性 context（不用
  persistent profile）、1920×1080 viewport——實測在 `cpu-basic`（2 vCPU、無 GPU、
  software WebGL）上最穩定；更大的 viewport（2560×1440 甚至 8K）在此硬體上會出現
  screenshot 逾時與日期讀取錯位。每個歷史日期約需 35–45 秒（無 GPU），`n_dates` 上限
  收緊為 12（約 10 分鐘封頂），避免單一任務佔滿容器過久。同一時間只允許一個擷取任務
  （行程內全域鎖），忙碌時新請求回 409。
- **進度回饋**：送出後顯示依「預估總時間」換算的進度條（時間常數依實測校準），並嘗試從
  即時 log 解析真實步驟（第幾期/第幾組變遷偵測）疊加顯示；進度條在真正完成前最多只到
  96%，避免時間估不準時誤報「完成」。
- **緊急停用開關**：`ENABLE_LIVE_CAPTURE`（預設 `1`）——設為 `0` 時 `/api/capture_custom`
  直接回 403、前端隱藏送出按鈕，改顯示已預先擷取好的示範案例（見 §5 豐丘觀測站）。

## 5. 分類地點瀏覽器（觀測導覽 + 線上分析共用）

`data/ardswc_hotspots/category_locations.json`，7 大類、131 筆真實地點，供「線上分析」
快速選點，也是「觀測導覽」landing page 卡片的資料來源。**每一筆座標都有可查證的真實
來源**（官方 PDF／政府開放資料集／維基百科），找不到精確座標的地點一律在說明文字中
如實標註「概略／未驗證」，不編造座標湊數。

| 分類 | 筆數 | 來源 |
|---|---|---|
| 水保署全台災害影像通報 | 100（既有熱點，此分類僅連結不重複列出） | photo.ardswc.gov.tw |
| 土石流觀測站 | 23 | 官方站點基本資料 PDF（dfm.ardswc.gov.tw），TWD97→WGS84 |
| 大規模崩塌潛勢區 | 94 | 政府開放資料 shapefile（246.ardswc.gov.tw／data.moa.gov.tw） |
| 海岸侵蝕熱點 | 2 | 僅收錄有可交叉查證精確座標者（外傘頂洲／七股潟湖） |
| 河川侵淤熱點 | 1 | 馬太鞍溪堰塞湖潰決案例（花蓮，2025） |
| 都市開發與違建監測 | 6 | 示範性行政區概略座標，非官方熱點清單 |
| 捷運興建前後變化 | 5 | 3 條路線中僅 1 條目前實際施工中，其餘為通車後對比案例 |

豐丘觀測站（土石流觀測站分類內）是唯一已預先擷取分析結果的示範案例——選取後直接顯示
既有比對結果，其餘 130 筆僅代填座標，交由使用者自行送出即時分析。

## 6. 巡查優先級 A–D

把「一個籠統的自動分數」轉成「下一步該做什麼」，依四項透明規則（非黑盒加權分數）：
複發年數、近期變遷分數、地形起伏（0.3km 內）、資料信心（人工覆核 > 自動對位良好 >
自動對位不確定 > 無資料）。任一因子為「未知」時，分級結果明確標示「資料待確認」，不
假裝有把握。

## 7. 其他功能模組

- **坡度與流域分析**：Cesium World Terrain 地形資料 → D8 流向＋流量累積，提示「可能
  積水/淹水」候選區——純地形幾何篩選，**不是水文水利模型**（無降雨強度/排水設施等
  資料，見前端固定顯示的治理警語）。
- **3D 立體檢視**：CSS `perspective`/`rotateX` 對地圖做視覺傾斜，**非真實 3D 相機**。
  傾斜期間精確點選（熱點點擊/自訂座標拾取）會暫停，因為透視畸變下螢幕像素↔地理座標的
  換算不再準確——寧可暫停功能也不要產出看似正常、實則有誤差的結果。
- **水保署歷史影像庫查詢**：右鍵選單的「水保署歷史影像庫」連到本站自建的 `/ardswc_search`
  路由，直接呼叫水保署公開資料 API（依分類＋年份分頁掃描，非操作該站帶 CSRF token 的
  搜尋表單），依「事件分類＋西元年份＋座標」現查真實候選，而非連到一個無法帶查詢條件的
  搜尋首頁。
- **等高線疊圖**：Cesium World Terrain（免費層級，非商用），`CESIUM_ION_TOKEN` 未設定時
  自動隱藏開關。

## 8. API 一覽

| 路由 | 方法 | 說明 |
|---|---|---|
| `/api/hotspots` | GET | 100 個熱點完整清單 |
| `/api/ledger` | GET | 11 個深度驗證熱點台帳 |
| `/api/priority` | GET | 巡查優先級 A–D＋逐項因子 |
| `/api/data_status` | GET | 權威資料檔的雜湊/筆數/修改時間快照 |
| `/api/events/stats`、`/api/events` | GET | 76,773 筆原始事件統計與地圖疊點 |
| `/api/hotspot_sites/<rank>`、`/api/timeline/<site>`、`/api/pair/<site>/<a>/<b>` | GET | 單一熱點的擷取站點/時間軸/單組比對結果 |
| `/api/category_locations` | GET | 7 大類 131 筆真實地點清單 |
| `/api/capture_status`、`/api/capture_custom`（POST）、`/api/job/<jid>`、`/api/site_dates/<site>` | — | 線上分析（自訂座標即時擷取，§4） |
| `/api/watershed/<rank>`、`/api/watershed_custom` | GET | 坡度與流域分析 |
| `/ardswc_search` | GET | 水保署歷史影像庫真實查詢（獨立 HTML 頁） |
| `/api/contours/<z>/<x>/<y>`、`/api/contours_status` | GET | 等高線疊圖 |
| `/api/tile/apple\|nlsc_topo\|nlsc_photo/<z>/<x>/<y>` | GET | 底圖圖磚代理（Google/ESRI/Bing/OSM 前端直連） |
| `/api/ge_trace` | GET | 依座標＋日期組出對應歷史日期的 GE Web 直連 URL |
| `/image/<relpath>` | GET | 靜態影像服務（`safe_join` 擋路徑穿越） |

## 9. 部署設定

`Dockerfile`：`python:3.11-slim-bookworm`（明確釘 Debian 版本——未釘版本的 `slim` 標籤
會隨時間滾動到新的 Debian 版本，曾導致 Playwright 依賴安裝失敗）+
`playwright install --with-deps chromium`。

環境變數（皆有安全預設值，未設定不影響核心功能）：

| 變數 | 預設 | 說明 |
|---|---|---|
| `CESIUM_ION_TOKEN` | 未設定 | 選配 Space secret；未設定時等高線/坡度流域分析自動隱藏 |
| `ENABLE_LIVE_CAPTURE` | `1` | 線上分析的緊急停用開關 |
| `GE_CAPTURE_CONTAINER_MODE` | `1`（此部署明確設定） | 切換擷取後端為容器安全模式（§4） |
| `GMAPS_DEMO_APPLE_AUTO` | `0`（此部署明確設定） | 關閉 Apple 底圖 token 自動換發（此容器連外部網站常逾時） |

## 10. 資料治理原則

- 所有自動化分數（含巡查優先級、SSIM 變遷分數）皆為**排序候選建議**，不代表災害已確定
  發生或其嚴重程度；人工覆核結論永遠優先。
- 分類地點清單（§5）的每一筆座標都經過真實來源查證——找不到精確座標的地點如實標註
  「概略／未驗證」，絕不編造座標湊數。
- 線上分析（§4）擷取的座標/日期為自動化幾何流程，非人工驗證的地面真相；產出的比對
  結果供快速排序參考，不取代現場複核。
- 坡度與流域分析（§7）為地形幾何篩選，非水文水利模型，不可用於估計實際淹水機率/深度。

## 11. 已知限制

- 本部署硬體無 GPU；線上分析每個歷史日期需 35–45 秒，多期擷取需耐心等候。
- 100 個熱點中僅 11 個有完整多期比對面板，其餘 89 個僅提供中繼資料與巡查優先級（點開
  會誠實顯示「尚無擷取資料」）。
- 分類地點清單非窮舉——例如海岸侵蝕熱點官方劃定 13 處，但僅 2 處找得到可交叉查證的
  精確座標；大規模崩塌潛勢區使用者常引用的「103」實為規劃篩選目標數，目前正式公告、
  有座標資料的是 94 處。
